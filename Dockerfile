# Используем готовый образ Node.js 14
FROM node:18-alpine

# Установка рабочей директории приложения внутри образа
WORKDIR /app

# Копируем файлы проекта в контейнер
COPY package*.json ./
COPY . .

# Установка зависимостей
RUN npm install

# Сборка приложения
RUN npm run build

# Указываем порт, на котором работает приложение
EXPOSE 3000

# Запуск приложения
CMD ["npm", "start"]